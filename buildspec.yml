version: 2.0
phases:
  install:
    runtime-versions:
      nodejs: 8
  build:
    commands:
      - echo "Nothing to do yet"
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/*
    - images/*
  